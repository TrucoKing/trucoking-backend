const express=require('express');const cors=require('cors');const bcrypt=require('bcrypt');const jwt=require('jsonwebtoken');const{Pool}=require('pg');const{MercadoPagoConfig,Payment}=require('mercadopago');require('dotenv').config();const app=express();const port=process.env.PORT||3000;app.use(cors({origin:'*',methods:['GET','POST','PUT','PATCH','DELETE','OPTIONS'],allowedHeaders:['Content-Type','Authorization','x-admin-key']}));app.options('*',cors());app.use(express.json());const db=new Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});const mpClient=new MercadoPagoConfig({accessToken:process.env.MP_ACCESS_TOKEN,options:{timeout:5000}});const mpPayment=new Payment(mpClient);async function setupDatabase(){await db.query(`CREATE TABLE IF NOT EXISTS users(id SERIAL PRIMARY KEY,nome VARCHAR(100) NOT NULL,cpf VARCHAR(14) UNIQUE NOT NULL,email VARCHAR(150) UNIQUE NOT NULL,telefone VARCHAR(20),senha_hash VARCHAR(255) NOT NULL,saldo DECIMAL(10,2) DEFAULT 0.00,avatar VARCHAR(50) DEFAULT 'smile',criado_em TIMESTAMP DEFAULT NOW());CREATE TABLE IF NOT EXISTS transacoes(id SERIAL PRIMARY KEY,user_id INTEGER REFERENCES users(id),tipo VARCHAR(20) NOT NULL,valor DECIMAL(10,2) NOT NULL,status VARCHAR(20) DEFAULT 'pendente',mp_payment_id VARCHAR(100),pix_chave VARCHAR(200),criado_em TIMESTAMP DEFAULT NOW());`);console.log('DB OK');}function auth(req,res,next){const token=(req.headers.authorization||'').replace('Bearer ','');if(!token)return res.status(401).json({erro:'Token necessario'});try{req.user=jwt.verify(token,process.env.JWT_SECRET);next();}catch{res.status(401).json({erro:'Token invalido'});}}app.get('/health',(req,res)=>res.json({ok:true}));app.post('/auth/cadastro',async(req,res)=>{const{nome,cpf,email,telefone,senha}=req.body;if(!nome||!cpf||!email||!senha)return res.status(400).json({erro:'Campos obrigatorios faltando'});if(senha.length<6)return res.status(400).json({erro:'Senha minimo 6 caracteres'});try{const hash=await bcrypt.hash(senha,10);const r=await db.query('INSERT INTO users(nome,cpf,email,telefone,senha_hash)VALUES($1,$2,$3,$4,$5)RETURNING id,nome,email,saldo,avatar',[nome,cpf,email,telefone||'',hash]);const user=r.rows[0];const token=jwt.sign({id:user.id,email:user.email},process.env.JWT_SECRET,{expiresIn:'7d'});res.json({token,user});}catch(err){if(err.code==='23505')return res.status(400).json({erro:'CPF ou email ja cadastrado'});console.error(err);res.status(500).json({erro:'Erro ao criar conta'});}});app.post('/auth/login',async(req,res)=>{const{email,senha}=req.body;try{const r=await db.query('SELECT * FROM users WHERE email=$1',[email]);const user=r.rows[0];if(!user)return res.status(401).json({erro:'Email ou senha incorretos'});const ok=await bcrypt.compare(senha,user.senha_hash);if(!ok)return res.status(401).json({erro:'Email ou senha incorretos'});const token=jwt.sign({id:user.id,email:user.email},process.env.JWT_SECRET,{expiresIn:'7d'});res.json({token,user:{id:user.id,nome:user.nome,email:user.email,saldo:user.saldo,avatar:user.avatar}});}catch(err){console.error(err);res.status(500).json({erro:'Erro ao fazer login'});}});app.get('/auth/me',auth,async(req,res)=>{const r=await db.query('SELECT id,nome,email,saldo,avatar FROM users WHERE id=$1',[req.user.id]);res.json(r.rows[0]);});app.patch('/auth/avatar',auth,async(req,res)=>{await db.query('UPDATE users SET avatar=$1 WHERE id=$2',[req.body.avatar,req.user.id]);res.json({ok:true});});app.post('/deposito/criar',auth,async(req,res)=>{const{valor}=req.body;if(!valor||valor<1)return res.status(400).json({erro:'Valor minimo R$ 1,00'});try{const payment=await mpPayment.create({body:{transaction_amount:Number(valor),description:'TrucoKing Deposito',payment_method_id:'pix',payer:{email:req.user.email},notification_url:(process.env.BACKEND_URL||'')+'/webhook/mp',metadata:{user_id:req.user.id,tipo:'deposito'}}});const tx=await db.query('INSERT INTO transacoes(user_id,tipo,valor,status,mp_payment_id)VALUES($1,$2,$3,$4,$5)RETURNING id',[req.user.id,'deposito',valor,'pendente',String(payment.id)]);res.json({transacao_id:tx.rows[0].id,qr_code:payment.point_of_interaction.transaction_data.qr_code,qr_code_base64:payment.point_of_interaction.transaction_data.qr_code_base64,valor});}catch(err){console.error(err);res.status(500).json({erro:'Erro ao gerar Pix'});}});app.get('/deposito/status/:id',auth,async(req,res)=>{const r=await db.query('SELECT * FROM transacoes WHERE id=$1 AND user_id=$2',[req.params.id,req.user.id]);if(!r.rows[0])return res.status(404).json({erro:'Nao encontrado'});res.json({status:r.rows[0].status,valor:r.rows[0].valor});});app.post('/webhook/mp',async(req,res)=>{res.sendStatus(200);const{type,data}=req.body;if(type!=='payment')return;try{const payment=await mpPayment.get({id:data.id});if(payment.status!=='approved')return;const userId=payment.metadata&&payment.metadata.user_id;const valor=payment.transaction_amount;const mpId=String(payment.id);const existe=await db.query('SELECT id FROM transacoes WHERE mp_payment_id=$1 AND status=$2',[mpId,'aprovado']);if(existe.rows.length>0)return;await db.query('UPDATE users SET saldo=saldo+$1 WHERE id=$2',[valor,userId]);await db.query('UPDATE transacoes SET status=$1 WHERE mp_payment_id=$2',['aprovado',mpId]);}catch(err){console.error(err);}});app.post('/saque/solicitar',auth,async(req,res)=>{const{valor,pix_chave}=req.body;if(!valor||valor<20)return res.status(400).json({erro:'Minimo R$ 20,00'});if(!pix_chave)return res.status(400).json({erro:'Informe sua chave Pix'});try{const r=await db.query('SELECT saldo FROM users WHERE id=$1',[req.user.id]);if(parseFloat(r.rows[0].saldo)<valor)return res.status(400).json({erro:'Saldo insuficiente'});await db.query('UPDATE users SET saldo=saldo-$1 WHERE id=$2',[valor,req.user.id]);await db.query('INSERT INTO transacoes(user_id,tipo,valor,status,pix_chave)VALUES($1,$2,$3,$4,$5)',[req.user.id,'saque',valor,'processando',pix_chave]);res.json({ok:true,mensagem:'Saque solicitado!'});}catch(err){console.error(err);res.status(500).json({erro:'Erro ao solicitar saque'});}});app.get('/carteira',auth,async(req,res)=>{const saldo=await db.query('SELECT saldo FROM users WHERE id=$1',[req.user.id]);const txs=await db.query('SELECT tipo,valor,status,criado_em FROM transacoes WHERE user_id=$1 ORDER BY criado_em DESC LIMIT 50',[req.user.id]);res.json({saldo:saldo.rows[0].saldo,transacoes:txs.rows});});app.get('/admin/usuarios',async(req,res)=>{if(req.headers['x-admin-key']!==process.env.ADMIN_KEY)return res.status(403).json({erro:'Acesso negado'});const r=await db.query('SELECT id,nome,email,saldo,criado_em FROM users ORDER BY criado_em DESC');res.json(r.rows);});setupDatabase().then(()=>{app.listen(port,()=>console.log('TrucoKing porta '+port));}).catch(err=>{console.error(err);process.exit(1);});
